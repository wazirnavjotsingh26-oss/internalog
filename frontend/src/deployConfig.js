/**
 * Your live Render web service URL (HTTPS, no trailing slash).
 * Copy it from: Render dashboard → your Web Service → URL
 *
 * If Vercel still shows "no server" / connection errors, this value MUST match
 * the host in frontend/vercel.json rewrites (update both to the same URL).
 */
export const RENDER_SERVICE_URL = 'https://intern-alog-012-1zd8.onrender.com'
